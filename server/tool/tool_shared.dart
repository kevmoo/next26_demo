  // For demo, defined in firebase.json
  const port = 5001;
  // For demo, defined in .firebaserc
  const projectId = 'demo-server';

  const functionBase = 'http://localhost:$port/$projectId/us-central1';
